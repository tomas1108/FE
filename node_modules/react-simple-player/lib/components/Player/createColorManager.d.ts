declare const createColorManager: (grey: [number, number, number], accent: [number, number, number]) => {
    isLight: boolean;
    avg: number;
    grey: [number, number, number];
    accent: [number, number, number];
    shade: (value: number) => string;
    shift: (value: number, opacity?: number) => string;
    contrast: (opacity?: number) => string;
};
export default createColorManager;
