import * as React from 'react';
export interface VolumeProps {
    value: number;
    bg: string;
    rail: string;
    railHover: string;
    onChange: (value: number) => void;
}
export declare const Volume: React.FC<VolumeProps>;
